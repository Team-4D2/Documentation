API
===

The API is built in fast API. There is a SQLite database using AIOSQLite. AIOSQLite is an asynchronous wrapper around SQLite, allowing for non-blocking database operations. 

.. autosummary::
   :toctree: generated

   lumache
