Frontend
========

The frontend is a Flutter application that provides a cross-platform mobile and web interface for UniFeed. It communicates with the FastAPI backend over HTTP and stores the user's authentication token locally using ``flutter_secure_storage``.

.. toctree::
   :maxdepth: 2
   :caption: Frontend Documentation

   frontend/setup
   frontend/screens
